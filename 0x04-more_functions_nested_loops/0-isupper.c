#include "main.h"
#include <stdio.h>
/**
* _isupper - Checks for uppercase character
* @c: character to be checked
* Return: Always 0 if c is uppercase and 0 otherwise
*/
int _isupper(int c)
{
return (c >= 'A' && c <= 'Z');
}
